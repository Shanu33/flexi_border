name: flexi_border
description: >
  FlexiBorder is a Flutter widget that draws fully customizable
  per-side borders—solid or dashed—with half/full lengths and per-side
  offsets.

version: 0.0.1
homepage: https://github.com/shanu33/flexi_border
repository: https://github.com/shanu33/flexi_border.git

environment:
  sdk: ">=3.7.0 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter
  dotted_border: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
